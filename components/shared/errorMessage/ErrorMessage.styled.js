import styled from "styled-components";

export const ErrorMessgeWrapper = styled.div`
    background: var(--color-primary);
    border-radius: 15px;
    color: var(--color-primaryLight);
    padding: 16px;
`;
